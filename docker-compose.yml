services:
  r-base:
    build: .manifests/r-base
    container_name: r-base
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    ports:
      - "8787:8787"
    environment:
      - PASSWORD=changeme